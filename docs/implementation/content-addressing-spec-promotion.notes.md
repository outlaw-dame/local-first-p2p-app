# Content Addressing Promotion Notes

This companion note intentionally keeps the promotion PR docs-only and narrow.

Follow-up checks:

- align glossary and registry terms;
- audit duplicate reference fields;
- map `StorageLocationHint` into future Series 7 provider descriptors;
- map `BundleRef` into Portable Sync Drop manifests.
