# Phase Map

This document reconciles the original Implementation Doctrine with what has actually been built so far.

The doctrine remains the target build-order authority. The code did not follow the doctrine order exactly because the repository first established a PWA/local-store/outbox/bridge foundation. That is acceptable as long as the deviation is explicit and future phases add the missing doctrine gates instead of drifting further.

## Status legend

- **Done for foundation**: enough exists for the current early foundation.
- **Partial**: a first slice exists, but doctrine-level scope is not complete.
- **Not started**: no meaningful implementation yet.
- **Deferred**: intentionally later.
- **Needs ADR**: more code should not proceed until a decision is recorded.

## Doctrine phase mapping

| Doctrine phase | Current status | Evidence in current code/docs | Gap before phase can be called complete |
|---|---:|---|---|
| Phase 0 - Doctrine, repo discipline, ADRs | Partial | Monorepo, CI, lint/typecheck/test/build, planning docs organized, ADR template, threat-model template, exit-report template, and protocol fixture policy added. | Explicit schema/storage versioning policy, ADR-000 runtime decision, and consistent use of exit reports on future phase claims. |
| Phase 1 - Protocol primitives and canonical fixtures | Partial | `SignedEventEnvelope`, `UnsignedEventEnvelope`, `SourceRef`, canonical JSON helper, `@lfp2p/content-addressing` digest primitives (DigestRef, ContentLink, BlockRef, ObjectRef, BundleRef), validation helpers, and protocol fixture policy. | Initial golden fixtures, independent hash/signature verification paths, unknown-version policy, negative fixture suite. |
| Phase 1.56 - Content addressing and object references | Foundation complete (package + parser shipped; integration deferred per plan) | `@lfp2p/content-addressing`: errors module with stable codes, hardened canonical JSON (prototype-pollution-safe, depth-bounded), `DigestRef` with algorithm-pinned length, BLAKE3 reserved in type system (fail-closed locally), CIDv1-only `ContentLink` with real multihash/multicodec binary parsing (varint + base32-lower + base16 decoders, multihash code allowlist with declared-length cross-check, trailing-byte rejection), `StorageLocationHint` with 13-kind enum and URL credential rejection, `BlockRef` with required encryption descriptor for private content and bounded compression descriptor (rejects bombs by ratio and absolute decoded size), `BundleRef` with format/purpose enums and non-empty roots, 12-kind discriminated `ObjectRef`, redaction helpers, 23-file fixture suite (11 valid + 12 invalid), 179 tests including 70+ adversarial cases. Exit report at `docs/implementation/phase-1.56-exit-report.md`. | Downstream integration: BLAKE3 runtime (needs ADR), full multibase decoders for `z` / `k` prefixes, application schemas built on these refs (belongs to 1.6/7/9), storage adapter wiring (belongs to 4/7/13), direct integration into `packages/protocol` envelopes (Step 2 of plan, intentionally deferred). |
| Phase 1.6 - Trust & Safety doctrine and protocol boundaries | Foundation complete (docs shipped; runtime split into 1.62–1.65) | ADR-004, `docs/protocol/trust-safety-event-policy.md`, `docs/threat-model/trust-safety-and-abuse.md`, `docs/implementation/trust-safety-phase-plan.md`. | Runtime slices live in phases 1.62 (local controls), 1.63 (reports/appeals), 1.64 (bridge admission), 1.65 (curation). |
| Phase 1.61 - Trust & Safety protocol core | Foundation complete (package shipped; runtime deferred per plan) | `@lfp2p/trust-safety`: stable `TS_*` errors, version-pinning validators that fail closed on unknown major versions, `SafetyAuthority` with scope/role/expiry cross-checks, 14-variant `SafetySubjectRef` (rejects URL credential injection, javascript: URLs, oversize URLs), `SafetyAction` split into moderation/curation/neutral with action-scope cross-validation (`reject-transport` requires a transport scope; curation actions cannot run at transport/network-advisory scope), `SafetyLabel` with confidence `[0,1]` and private-subject + public-scope leak rejection, `SafetyLabelDefinition` with hard-safety downgrade rejection (hardSafety cannot pair with permissive defaultAction or with userConfigurable), `SafetyLabelerProfile` with https-only credentialless service endpoint, `SafetyReport` with required idempotency key and private-subject + public-scope leak rejection, `SafetyAppeal` targeting decisions, `SafetyPolicyDecision` with action/scope and private-subject cross-checks, `TransportAdmissionDecision` requiring infrastructure operator authority with surface/scope cross-check, `CurationRule` + `CurationExplanation` with `TS_CURATION_MASQUERADE` rejection so moderation actions cannot smuggle in via curation. Consumes `@lfp2p/content-addressing` for `ObjectRef`/`BlockRef`/`DigestRef`. 18 valid + 15 invalid fixtures, 137 tests including adversarial cases. Exit report at `docs/implementation/phase-1.61-exit-report.md`. | Phase 1.62 local user controls, Phase 1.63 reports/appeals runtime, Phase 1.64 bridge admission runtime, Phase 1.65 curation runtime, capability/credential verification, identity-control authority resolution. |
| Phase 1.62 - Local user controls | Foundation complete with cross-app portability layer (Dexie persistence + PWA wiring + account-local sync envelope deferred) | `@lfp2p/trust-safety` local-controls slice: 12 event kinds total — 7 baseline (`safety.account.blocked`, `safety.account.muted`, `safety.domain.blocked`, `safety.keyword.muted`, `safety.thread.muted`, `safety.post.hidden`, `safety.label.preference.set`) plus 5 portability/expansion (`safety.account.allowlisted`, `safety.policy-list.subscribed`, `safety.policy-list.unsubscribed`, `safety.notification-preference.set`, `safety.preferences.snapshot`). Optional `expiresAt` on every applicable event; selector takes `now` and skips expired entries; `pruneExpiredLocalControlState` for compaction. `safety.keyword.muted` supports `substring`, `word`, and `semantic` match kinds (no regex anywhere; semantic carries embeddingRef + embeddingModel + similarityThreshold and is evaluated by a host-supplied callback so the package stays ML-free). `decideVisibility` supports allowlist that suppresses non-hard-safety labels while preserving user blocks/mutes/keywords and hard-safety labels. Cross-app portability via `exportPreferencesSnapshot` / `importPreferencesSnapshot` with three merge strategies (union, replace, merge-newer-wins), schema-pinned validator, and `assertSnapshotIsNotStale` guard — solving the Nostr-style "preferences scatter across apps" problem at the protocol layer. Doctrine: `docs/protocol/local-controls-portability.md`. `assertLocalControlEnvelopeScope` continues to fail-closed every non-private scope with `TS_PRIVATE_LEAK`. 15 valid + 11 invalid fixtures; 100 local-controls tests including TTL expiry, allowlist precedence, semantic matcher injection, snapshot round-trip and stale rejection, ReDoS guard, prototype-pollution guard. Exit report at `docs/implementation/phase-1.62-exit-report.md`. | Dexie projection persistence (local-store), PWA settings UI (apps/pwa), account-local sync envelope (ADR-002 + sync-client), bridge-side transport enforcement of `safety.account.blocked` (Phase 1.64), host-side semantic embedding pipeline (PWA/host concern). |
| Phase 1.63 - Reports, appeals, and encrypted evidence refs | Foundation complete (events + projection + privacy guards shipped; bridge/curation/persistence runtimes deferred per plan) | `@lfp2p/trust-safety` reports-appeals slice: 5 lifecycle events (`safety.report.created`, `safety.report.acknowledged`, `safety.report.resolved`, `safety.appeal.created`, `safety.appeal.resolved`), pinned `lfp2p.report-appeal-event.v1`, new stable error code `TS_LIFECYCLE_TRANSITION`. State machine enforced at apply time (report: submitted → acknowledged → resolved with submitted-→-resolved skip-ack allowed; appeal: submitted → resolved; terminal states; illegal transitions throw fail-closed). `ReportsAppealsState` frozen snapshot with six indexes (`byReportId`, `byReportIdempotencyKey`, `byTargetAuthority`, `byAppealId`, `byAppealIdempotencyKey`, `byAppealedDecisionId`) plus `appliedEventIds`; idempotency-key duplicates on `safety.report.created` are silent no-ops at the projection layer. `assertPrivateEvidenceOnPrivateSubject` enforces: private-by-nature subjects (blob/media/thread) require media evidence with `privacy=private` + encryption descriptor, bundle evidence with `encrypted=true`, and reject identity-kind ObjectRefs as encryptedBodyRef. Guard runs BEFORE projection mutation so unsafe events cannot land. `canBridgeForwardReport(report)` is a structural pre-check usable by Phase 1.64. Drive-by fix: removed `'media'` from `ContentBackedKind` in `@lfp2p/content-addressing` to make ObjectRef discriminated-union narrowing work cleanly. 5 valid + 4 invalid fixtures; 50 new tests covering every lifecycle transition (legal and illegal), idempotency dedup, replay equivalence, store-reopen rebuild, privacy classification, encrypted-evidence enforcement at both helper and projection layers, escalated-without-target rejection, overturned-without-newDecisionId rejection. Exit report at `docs/implementation/phase-1.63-exit-report.md`. | Phase 1.64 bridge admission runtime (must call `canBridgeForwardReport` before forwarding and must not decrypt encrypted bodies), Phase 1.65 curation runtime (must not ingest `private-only` reports), Dexie persistence (local-store), signature verification at envelope layer, capability/credential-based authority resolution (ADR-006 trust-policy engine). |
| Phase 1.64 - Bridge / relay / super-peer admission policy | Foundation complete (engine + projection + doctrine shipped; bridge-service HTTP wiring + Dexie + signature verification deferred per plan) | `@lfp2p/trust-safety` transport-admission slice: 6 transport events (`transport.event.accepted|rejected|quarantined`, `transport.peer.rate_limited|quarantined`, `transport.media.rejected`), pinned `lfp2p.transport-event.v1`. Pure token-bucket rate limiter with **real exponential backoff** (`baseBackoffMs * 2^(consecutiveRefusals-1)`, capped) and integer-floor refill (no fractional-token exploit); refusals during cooldown do NOT escalate; successful admit resets counter (self-healing). Peer reputation tracker with time-based decay toward 0 (never crosses zero), bounded score `[-1000, +1000]`, hysteresis-gated auto-quarantine (enter at quarantineThreshold, lift at recoveryThreshold or after maxQuarantineMs TTL), negative-zero normalized. Replay cache with TTL + oldest-first eviction so flood attack cannot OOM the bridge or retain entries past TTL. Audit log with `redactDigestRef` truncation (8-char prefix), encryption-key refs dropped entirely, CIDs truncated, **whole-second timestamps** to avoid timing-oracle fingerprints, FIFO eviction at capacity. Admission decision engine with 10 ordered checks (schema → replay → privacy scope per surface → kind allowlist → byte size → decoded-size compression-bomb guard → peer quarantine → rate limit → user-block transport [1.62 deferral] → report-forwarding privacy guard [1.63 deferral]). Per-surface privacy scope allowlists: bridge/relay/media-store `{dm,group,public}`, super-peer `{group,public}`, public-index `{public}`. Phase 1.62 deferral: `decideUserBlockTransport` refuses to forward producer→recipient when producer is in recipient's `blockedActors` (TTL-aware). Phase 1.63 deferral: `decideReportForwarding` runs structural `canBridgeForwardReport` check without decrypting anything. `TransportAdmissionState` frozen snapshot with `peerReputation`, `rateLimitState`, `replayCache`, `quarantinedPeers/Events/Media`, `auditLog`, `appliedEventIds`; `admitEnvelope` is the canonical entry point; `applyTransportEvent` for rebuild from log; `seedTransportAdmissionState` for store reopen. 4 valid + 2 invalid fixtures; 60+ new tests including rate-limit exponential growth, cap, cooldown semantics, self-healing, reputation decay & hysteresis & TTL, replay-cache flood resistance, audit-redaction completeness, admission engine per check, deferral integrations. Doctrine: `docs/protocol/bridge-admission-doctrine.md` with non-negotiable rules + check-order spec + "what bridge MUST NOT do" section. Exit report: `docs/implementation/phase-1.64-exit-report.md`. | `apps/bridge-service` HTTP wiring of `admitEnvelope`, Dexie persistence of `TransportAdmissionState`, envelope-layer signature verification, policy-list resolution runtime (Phase 1.62 subscriptions), media-scanner verdict integration, multi-bridge advisory reputation propagation, durable audit-log persistence. |
| Phase 1.65 - Curation and reach controls | Foundation complete (events + projection + surface gate + doctrine shipped; feed/search runtime + Dexie persistence + trust-policy integration deferred per plan) | `@lfp2p/trust-safety` curation-runtime slice: 6 lifecycle events (`curation.rule.created | disabled`, `curation.item.boosted | downranked | excluded`, `curation.explanation.recorded`), pinned `lfp2p.curation-event.v1`. Score deltas bounded `[0, MAX_SCORE_DELTA=100]` non-negative safe integer per event so a single rule cannot dominate the ranking and an adversary cannot push the projection into a non-recoverable state. `excludeFrom: 'feed' \| 'search' \| 'recommendation'` per exclusion event so a single exclusion cannot target all three surfaces at once — enforces the doctrine distinctions structurally. Rule state machine `active → disabled` (terminal); re-creation under existing `ruleId`, double-disable, and disable-unknown all throw `TS_LIFECYCLE_TRANSITION`. `computeItemRanking(state, subject)` returns `effectiveNetScoreDelta` and per-surface exclusion flags **filtered to currently-active source rules** — disabling a rule immediately retracts its effect from the ranking view without rewriting history. `decideCurationSurfaceIngest(surface, envelopeScope, subject)` is the public-surface gate: rejects non-public envelope scopes on `public-feed`/`search`/`recommendation`, rejects private-by-nature subject types on public surfaces; local surfaces accept any scope. **Phase 1.63 deferral resolved**: `decideReportAsCurationSignal(report, surface)` refuses to use a `private-only` report as a curation signal on public surfaces. `subjectKey` stably encodes any `SafetySubjectRef` for record keys; private-by-nature subjects key by source digest body / CID, never the encryption-key digest. **Hardening pass folded in**: `assertId` rejects reserved property names (`__proto__`, `prototype`, `constructor`, `hasOwnProperty`, etc.) with new `TS_FORBIDDEN_KEY`; shared `projection-helpers.ts` uses `Object.defineProperty` so even bypassed forbidden keys cannot mutate the prototype chain. All three prior projections (local-controls, reports-appeals, transport-admission) refactored to use the shared helpers; no duplicate code. 6 valid + 3 invalid fixtures; 90+ new tests including hardening cross-projection (29 tests), curation events, projection state machine, distinctions, ranking filter-on-disable, surface gate matrix, private-only report refusal. Doctrine: `docs/protocol/curation-doctrine.md` with five non-negotiable distinctions, surface gate spec, "what curation runtime MUST NOT do". Exit report: `docs/implementation/phase-1.65-exit-report.md`. | `@lfp2p/search` and feed runtime consuming `computeItemRanking`, Dexie persistence of `CurationState`, cross-rule attribution analytics, trust-policy engine integration (ADR-006). |
| Phase 1.66 - Labeler runtime (composable / stackable) | Foundation complete (events + projection + kind taxonomy + aggregators + doctrine shipped; labeler API + Dexie + trust-policy integration deferred) | `@lfp2p/trust-safety` labelers-runtime slice: 7 lifecycle events (`safety.labeler.profile.published`, `safety.label-definition.published`, `safety.labeler.subscribed | unsubscribed`, `safety.label.applied | revoked`, `safety.annotation.created`), pinned `lfp2p.labeler-event.v1`. `SafetyLabelerProfile` extended with optional `kind: LabelerKind` (7 values: `human-curated`, `automated-classifier`, `hybrid`, `attestation`, `community-aggregator`, `media-scanner`, `unknown`) and optional `aggregatorOf` for community-aggregator labelers (cross-validated: required for aggregators, rejected for non-aggregators, no self-loops). State machine: profile re-publish supersedes; subscription `active → unsubscribed` terminal; label `active → revoked` terminal with **cross-labeler revoke rejected** (only the original issuing actor can revoke its own labels). Composable stacking: `effectiveLabelsForSubject` returns `ResolvedLabel[]` per (labelKey × issuing labeler) with full provenance (issuer actorId, labelerId, labelerKind, severity, confidence, effectiveAction); `mostRestrictiveAction` is the default combiner. Improvements over ATProto: private-by-default subscriptions (Phase 1.62 carryover), per-namespace + per-label trust, hard-safety can't be downgraded, first-class aggregators, explicit transparent stacking. 6 valid + 3 invalid fixtures; 27 new tests including 2-labeler stacking, per-labeler override, cross-labeler revoke rejection, unsubscribed mid-stack, namespace + label filters, profile re-publish, replay equivalence, aggregator cross-checks (without-sources / self-loop / non-aggregator-with-field). Doctrine: `docs/protocol/labeler-runtime-doctrine.md` with ATProto mapping + 12 improvements. Exit report: `docs/implementation/phase-1.66-exit-report.md`. | Labeler HTTP/WS API (future `apps/labeler-service`), subscriber-side ingestion runtime (`packages/sync-client`), Dexie persistence, trust-policy engine integration. |
| Phase 1.67 - Moderation runtime (policy + queue + decision lifecycle) | Foundation complete (events + projection + doctrine shipped; moderation tools API + Dexie + cross-projection automation deferred) | `@lfp2p/trust-safety` moderation-runtime slice. New shape `SafetyPolicy` (`lfp2p.safety-policy.v1`) with versioned policy chain (policyId, policyVersionNumber, supersedesPolicyVersionNumber cross-validated to be < versionNumber). 7 lifecycle events (`safety.policy.created | updated | deprecated`, `safety.policy.decision.recorded`, `moderation.queue.item.created | assigned | resolved`), pinned `lfp2p.moderation-event.v1`. Policy state machine: `absent → active@v1 → active@v2 ...→ deprecated@vN` (terminal); updates require `policyVersionNumber = active + 1` AND `supersedesPolicyVersionNumber = active`; deprecation does NOT retroactively reverse decisions (audit-preserving). Queue state machine: `open → assigned → resolved` with `open → resolved` skip-assignment permitted; terminal at resolved. Decision recording: append-only by `decisionId`; duplicate silent no-op. Rich indexing: `decisionsBySubjectKey`, `decisionsByPolicyId`, `queueIdsByStatus` (open/assigned/resolved buckets), `queueIdsByAssignee` (moderator inbox), `queueIdsBySourceId` (cross-ref from `(sourceKind, sourceId)` → queue items). Two-way linkage: queue resolution cites `resolutionDecisionId`; decision references `sourceQueueItemId`. Phase 1.63 integration: `queueItemsForSource('report', reportId)` returns all queue items spawned from a report. 3 valid + 3 invalid fixtures; 28 new tests covering every state machine transition (legal and illegal), version chaining, deprecation preserves history, queue/decision two-way linkage, replay equivalence, eventId idempotency. Doctrine: `docs/protocol/moderation-runtime-doctrine.md` with five non-negotiable rules, state machines, cross-reference index table, integration points, "what moderation runtime MUST NOT do". Exit report: `docs/implementation/phase-1.67-exit-report.md`. | Moderation tools API (future `apps/moderation-tools`), Dexie persistence, cross-projection automation (report.acknowledged → queue.item.created), trust-policy engine integration. |
| Phase 1.68 - T&S completion sweep | Complete (docs-only) | Threat model refreshed (`docs/threat-model/trust-safety-and-abuse.md`) with implementation-update section covering Phase 1.62.1 expansion, 1.64 deferral integrations, 1.65 surface gate, 1.65 hardening pass, 1.66 labeler runtime, 1.67 moderation runtime. New canonical entry point `docs/implementation/trust-safety-complete-summary.md` surveying the full 1.6x stack: phases shipped, sub-modules with public surface + doctrine doc, dependency graph (no cycles), per-consumer "how to consume" sections (PWA, subscriber-side label ingestion, bridge/relay/super-peer, moderation tools, public feed/search/recommendation), explicit non-deferred deferrals table with target downstream packages, boundary discipline statement, final acceptance criteria checklist (all ticked except Phase 7 contingency). Exit report: `docs/implementation/phase-1.68-exit-report.md`. | (none — docs sweep) |
| Phase 1.69 - Content categories, labeler capabilities, overlap detection | Foundation complete (registry + capability declarations + adult-content gate + overlap helpers + doctrine shipped; labeler API + moderation-tools UI deferred) | `@lfp2p/trust-safety` four gap closures matching Bluesky's content-filter UX. **Content categories**: new module `@lfp2p/trust-safety/content-categories` with `CONTENT_CATEGORY_NAMESPACE = 'lfp2p.content-category.v1'` and 20 standard categories (`adult.sexually-explicit`, `adult.sexually-suggestive`, `adult.nudity-artistic`, `violence.gore/graphic/threat`, `self-harm`, `eating-disorder`, `hate.iconography/slur`, `intolerance.targeted`, `spam`, `impersonation`, `misleading-claim/context`, `bot-account`, `political`, `religion`, `gambling`, `screenshot.crossplatform`) each with `isAdult`, conservative `defaultAction` (Bluesky-style: spam/hate/impersonation/violence.threat default `hide`; politics/religion default `allow`; ambiguous default `warn`), description. `ADULT_CONTENT_CATEGORY_KEYS` set. `decideContentCategoryAction(category, userPreference, gateEnabled)` resolver: adult categories force `hide` when gate is off (cannot be overridden). **Labeler capabilities**: optional `capabilities: LabelerCapability[]` added to `SafetyLabelerProfile` (additive v1). Capability shape: `{ capabilityId, description, producesLabels, mediaTypes? }`. `capabilityId` pattern `(detect|classify|scan|attest|aggregate|x).<segment>(.<segment>)*` with `x.*` allowed for custom community extensions. Cross-validated: `producesLabels` must be a subset of `profile.supportedLabels`; non-empty required; `mediaTypes` must be RFC-6838. `STANDARD_LABELER_CAPABILITIES` registry of 20 IDs (`detect.twitter-screenshot`, `detect.bluesky-screenshot`, `detect.profanity-en`, `classify.spam`, `classify.toxicity`, `classify.adult-content`, `scan.media-csam`, `attest.domain-ownership`, `aggregate.community-list`, …). `isStandardCapability` predicate. **Adult-content master gate**: new local-control kind `safety.adult-content.gate.set` (13th kind), payload `{ enabled, gatedAt }`. Projected onto `LocalControlState.adultContentGate`; `apply` records, `revert` clears. `assertLocalControlEnvelopeScope` continues to reject any non-private scope with `TS_PRIVATE_LEAK`. **Overlap detection**: new `labelers-runtime/overlap.ts` module. `findOverlappingSubscriptions(state, subscriberActorId)` returns pairs of *active* subscribed labelers with `level` of `full` (subset capabilities), `partial`, `capability-only`, or `label-only`. `detectRedundantSubscription(state, subscriberActorId, candidateLabelerId)` is the pre-subscribe UI warning helper. Behavioural pinning: Twitter-screenshot + profanity labeler → no warning; second `classify.spam` → warning. 2 new fixtures (`profile-with-capabilities.json`, `adult-content-gate-enabled.json`). 27 new tests in `phase-1.69.test.ts` covering every capability cross-check, the decision matrix, gate apply/revert, all 4 overlap levels, and the user-prompt scenarios verbatim. Doctrine: `docs/protocol/content-categories-doctrine.md` with Bluesky mapping table, full 20-category reference, gate semantics, capability namespace spec. Exit report: `docs/implementation/phase-1.69-exit-report.md`. | Labeler HTTP/WS API + per-platform screenshot ML implementations, moderation-tools UI presenting the overlap warning + category preferences, per-actor/per-thread overrides of the adult gate, community-list discovery / recommendation surface. |
| Phase 1.70 - PWA T&S settings + hashtag/phrase match kinds | Foundation complete (PWA settings surface + Dexie persistence + protocol additions shipped; labeler discovery flow + semantic embedding pipeline deferred) | `@lfp2p/trust-safety` adds `phrase` and `hashtag` to `KEYWORD_MATCH_KINDS` as first-class, ReDoS-safe match kinds: `phrase` trims + whitespace-collapses + case-insensitively substring-matches; `hashtag` body must match `^[\p{L}\p{N}_]{1,140}$` (Unicode), is stored without `#` and lowercased, and matches by char-by-char scan against `#tag` tokens in the haystack. **Regex deliberately not added** — author-supplied general regex is a documented ReDoS vector (Bluesky, Mastodon server filters, X all decline it); adversarial timing tests pin the new matchers at under 200 ms on 20 000-char payloads that would catastrophically backtrack against a naive `(?:a+)+$`. `@lfp2p/local-store` adds Dexie schema v7 with two append-only event-log tables (`trustSafetyControlEvents`, `trustSafetyLabelerEvents`) keyed by `eventId` with monotonic `sequence` and validate-on-read defense-in-depth; `appendTrustSafetyControlEvent` / `appendTrustSafetyLabelerEvent` are idempotent on `eventId`, transactional, and re-validate at the persistence boundary; `loadLocalControlState` / `loadLabelersState` replay the log through the protocol's pure projection. PWA settings surface (`apps/pwa/src/pwa-trust-safety-state.ts` + `pwa-trust-safety-settings.tsx`) renders the Bluesky-equivalent T&S controls: adult-content master gate toggle (with explicit 18+ confirm before enabling), 20-category preference list (Show/Warn/Hide; adult rows show locked indicator when gate off), keyword filter add/remove with the 4 UI match-kinds + explicit "regex is intentionally not offered" note, labeler subscription list with `findOverlappingSubscriptions` warning callout and `detectRedundantSubscription` pre-subscribe assessment. 4 new fixtures (2 valid `keyword-muted-phrase.json` / `keyword-muted-hashtag.json` + 2 invalid `keyword-hashtag-with-whitespace.json` / `keyword-phrase-empty-after-trim.json`). 70 new tests (32 protocol + 6 persistence + 32 PWA state). Doctrine note appended to `docs/protocol/local-controls-portability.md` covering the ReDoS guard on the new kinds. Exit report: `docs/implementation/phase-1.70-exit-report.md`. | Labeler discovery / publish surface in the PWA (the settings screen lists subscriptions but does not yet offer a "find a labeler" flow), semantic keyword filters in the UI (need an embedding pipeline; protocol already accepts the kind), per-actor / per-thread overrides of the adult gate, moderation-tools UI surfacing category labels alongside keyword hits. |
| Phase 2 - Identity control log v1 | Partial/early — protocol core done (Phase 2.1) | Local device identity bootstrap exists with encrypted private-key material. ADR-001 records root/controller identity and identity-control model constraints. **Phase 2.1 shipped (2026-06-03)**: `@lfp2p/identity` now has the same shape guarantees as `@lfp2p/trust-safety` — stable `IDENTITY_*` error namespace (22 codes), pure `validateIdentityEvent` shape validator separate from the projection, prototype-pollution defense at every payload-object boundary, public-key wire-format check, digest-reference wire-format check, 16 KB serialized-payload cap. 7 canonical event kinds: `controller.created`, `device.authorized`, `device.revoked`, **`device.rotated`** (new — in-place key swap with `previousPublicKey` cross-check against stored key, rejects revoked devices, rejects same-key reuse, monotonic epoch), `capability.granted`, `capability.revoked`, **`contact-card.published`** (new — formalizes audit trail for the PWA's contact-card publication; projection retains the most recent digest). Projection re-validates identity events with the pure validator before any mutation (defense-in-depth). 7 valid + 6 invalid fixtures including a JSON-parse-delivered `__proto__` prototype-pollution test; 39 new tests in `phase-2.1.test.ts`. Doctrine: `docs/protocol/identity-control-log.md`. Exit report: `docs/implementation/phase-2.1-exit-report.md`. | Phase 2.2 — projection persistence in `@lfp2p/local-store` (event-log table + idempotent append + replay-on-open) + PWA emit/append wiring for the existing identity flows; account recovery flow (controller-key replacement); capability delegation chains (delegate-of-delegate); multi-controller accounts; identity-specific threat-model document. |
| Phase 3 - PWA light peer foundation | Done for foundation | Framework7 PWA shell, Dexie store, local signing, local event append, local view summary, mutation outbox. | Service worker, privacy-safe logging policy, local view rebuild tests, resume/network retry wiring. |
| Phase 4 - Bridge and infrastructure classes v1 | Partial — Phase 4.1 planned next | Bridge service primitives, request handler, signature verification, bridge-safe scope checks, in-memory/JSON/PGlite stores, HTTP sync-client transport. | **Phase 4.1 — wire the existing trust-safety transport-admission engine into `apps/bridge-service`** (the engine, fixtures, and tests already exist from Phase 1.64; the bridge just doesn't call them yet — this closes the documented 1.64 deferral and is the smallest concrete bridge slice we can ship next); production bridge runtime; auth/rate limiting; encrypted mailbox/resumable stream; offsets/cursors; durable-stream/WebSocket reader. |
| Phase 5 - Chat vertical slice | Not started — blocked on Phase 5.0 | Protocol has placeholder event kinds but no room model. | **Phase 5.0 — ADR-002 private payload envelope implementation** is a hard prerequisite (chat requires the encryption envelope; ADR exists, runtime does not); room metadata/events; deterministic room view; private plaintext prohibition; bridge-delivered room updates; T&S local controls/reporting gates. **Class D events in `docs/protocol/operation-consistency-classes.md` arrive with this slice.** |
| Phase 6 - MLS private group encryption v1 | Not started | No MLS code. | MLS library evaluation, virtual Delivery Service ADR, control log, fork detection, PWA client constraints, report/evidence path for encrypted groups. |
| Phase 7 - Media manifests and chunked storage v1 | Not started — blocked on Phase 7.0 | No media manifest or media package yet. | **Phase 7.0 — `packages/block-store` runtime is a hard prerequisite** (the content-addressing validators exist from Phase 1.56; a runtime that uses them at fetch time with `fetch → cap → verify-digest → decode` discipline does not. Block-store also unblocks encrypted-evidence retrieval from Phase 1.63 and is reusable for Phase 5 chat attachments). Manifest v1 built on `BlockRef`/`ObjectRef`; image pipeline; storage abstraction; private/public replication policy; hash/CID validation; media safety/quarantine policy. |
| Phase 8 - Ephemeral presence plane | Not started | No presence model. | TTL events outside durable logs, rate limiting, stale sequence handling, WebRTC/bridge fallback policy. |
| Phase 9 - Social outbox v1 | Not started | Only local outbox test events exist. | Public outbox event model, post/comment/reaction/follow/repost events, backlinks, spam/quarantine buckets, T&S/reach controls. |
| Phase 10 - Human naming and NIP-05-like proofs | Not started | Protocol includes `contact.petname.set`, but no storage/UI/name proof model. | Petname book, contact cards, namespaces, reciprocal claims, expiration/revocation, anti-phishing UI, T&S impersonation handling. |
| Phase 11 - Local search and intelligence v1 | Partial/early | PGlite search projection and basic escaped text search. | SearchObject, source provenance via `ObjectRef`, permissions, proper lexical ranking, semantic adapter, embedding lifecycle, hybrid fusion, curation/exclusion controls. |
| Phase 12 - Compression, chunking, and dedupe | Not started | No compression descriptors. | CompressionDescriptor ADR, zstd/gzip policy, decoded-size limits, dictionary trust rules, dedupe-scope policy, integration with `BlockRef`. |
| Phase 13 - Native/Bare full peer prototype | Deferred | Repo boundaries are future-compatible but no full peer implementation exists. | Corestore/Hypercore/Autobase/Hyperbee/Hyperdrive/Hyperswarm adapter prototype and compatibility fixtures using protocol-level refs, not backend-specific identities. |
| Phase 14 - Hardening and beta readiness | Not started | Some bridge/outbox hardening exists. | Threat models, malicious peer tests, network partition tests, media abuse tests, revocation chaos tests, rollback/migration plan. |

## Practical current phase

The repository is effectively between:

- **Phase 0 partial**: process scaffolding now exists, but ADR-000 and explicit versioning policy still need to be written.
- **Phase 1 partial**: protocol primitives exist but initial fixture discipline is not yet implemented in tests.
- **Phase 1.56 foundation complete**: `@lfp2p/content-addressing` shipped with errors, hardened canonical JSON, algorithm-pinned `DigestRef`, BLAKE3 reservation, CIDv1 binary parsing (multibase decode → version → multicodec → multihash with declared-length cross-check → no trailing bytes), `StorageLocationHint`, `BlockRef`, `BundleRef`, 12-kind `ObjectRef`, redaction. 179 tests including 70+ adversarial cases; 23-file fixture suite. Exit report: `docs/implementation/phase-1.56-exit-report.md`. Downstream integration (BLAKE3 runtime, protocol-envelope wiring, T&S/media/bridge consumption) is intentionally deferred to later phases per the plan's integration sequence.
- **Phase 1.6 / 1.61 foundation complete**: `@lfp2p/trust-safety` shipped with stable `TS_*` errors, version-pinning validators (fail-closed on unknown major versions), `SafetyAuthority` with scope/role/expiry cross-checks, 14-variant `SafetySubjectRef` (rejects URL credential injection / javascript: URLs / oversize URLs), `SafetyAction` split into moderation vs curation with action/scope cross-validation, hardened `SafetyLabel`/`SafetyLabelDefinition`/`SafetyLabelerProfile`/`SafetyLabelerSubscription`/`SafetyAnnotation`/`SafetyReport`/`SafetyAppeal`/`SafetyPolicyDecision`/`TransportAdmissionDecision`/`CurationRule`/`CurationExplanation`. Privacy guards reject private-by-nature subjects at public scopes. Curation masquerade guard prevents moderation actions sneaking in via curation. 18 valid + 15 invalid fixtures; 137 tests including adversarial cases. Exit report: `docs/implementation/phase-1.61-exit-report.md`. Runtime slices (1.62 local controls, 1.63 reports/appeals, 1.64 bridge admission, 1.65 curation) intentionally deferred per the plan.
- **Phase 3 foundation complete enough**: the PWA can create signed local events and queue outbox writes.
- **Phase 4 partial**: bridge transport and bridge stores exist, but production infrastructure, sync offsets, and T&S admission controls are not complete.

## Recommended next phase label

Use this working label for the next development cycle:

> **Phase 1.5 / 3.5 - Doctrine alignment and protocol hardening before feature expansion**

This cycle should use the newly added scaffolding and complete the remaining guardrails before adding chat/media/search breadth.

The content-addressing and trust/safety docs define two explicit gates inside this cycle:

1. **Phase 1.56 - Content Addressing and Object Reference Model** (foundation complete — package done, downstream integration deferred)
2. **Phase 1.6 / 1.61 - Trust & Safety Protocol Core** (foundation complete — package done, runtime deferred to 1.62–1.65)

## Next required gates

1. Record ADR-000 for the runtime/product decision.
2. Add an explicit schema/storage versioning policy.
3. Add initial protocol event fixtures and tests under the fixture policy.
4. Add current bridge compromise threat-model note.
5. Add sync offset/checkpoint design before implementing Durable Streams/WebSocket readers.
6. Expand identity-control implementation from ADR-001 into protocol fixtures and projection logic.
7. Expand payload encryption implementation from ADR-002 into envelope schema, fixtures, and enforcement.
8. Complete Phase 1.56: app-specific schemas, CID wrappers, storage adapters, protocol event integration, and golden fixture set. (`packages/content-addressing` core is done.)
9. ~~Implement Phase 1.6/1.61: `packages/trust-safety`, validators, fixtures, and tests.~~ Done — see `docs/implementation/phase-1.61-exit-report.md`.
10. ~~Implement Phase 1.62 local user controls before networked moderation queues or public labelers.~~ Done — see `docs/implementation/phase-1.62-exit-report.md`.
11. ~~Implement Phase 1.63 report/appeal/evidence refs before public media, search, social outbox, or production bridge deployment.~~ Done — see `docs/implementation/phase-1.63-exit-report.md`.

## Development warning

Do not interpret the current local device identity implementation as the full identity architecture. It is an early local bootstrap slice. The doctrine-level identity model still requires root/controller authority, device delegations, revocation, capabilities, and epochs.

Do not interpret content addressing as IPFS adoption. CIDs/content links are identifier semantics, not network/storage authority.

Do not interpret trust and safety as a centralized global moderation service. T&S decisions are scoped, auditable, and tied to local user policy, community policy, infrastructure self-protection, and future capability/credential authority.
